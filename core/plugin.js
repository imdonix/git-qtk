const { abs } = require('./utils')

class Plugin
{
    name() { abs() }

    functions() { abs() }

    reductors() { abs() }

    models() { abs() }

    async init() { abs() }

    async parse(db, commit) { abs() }

    async post() { abs() }
}

module.exports = Plugin