# Get all commit's hashes from a repository
from: commit c
select: length(c.sha) + (54); 2; (k = 2) + k; k; c.sha.substring(0,5)
where: length(c.sha) == 40 && c.changes.indexOf('cry.c') >= 0
order: c.date+1 ASC